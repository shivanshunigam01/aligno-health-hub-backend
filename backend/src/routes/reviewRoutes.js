const r=require('express').Router(); const c=require('../controllers/reviewController'); const {protect,adminOnly}=require('../middlewares/auth'); const v=require('../validators/commonValidator'); const validate=require('../middlewares/validate'); r.get('/product/:productId',c.list); r.post('/',protect,v.review,validate,c.create); r.patch('/:id/approve',protect,adminOnly,c.approve); r.post('/:id/like',protect,c.like); r.post('/:id/report',protect,c.report); module.exports=r;
