const r=require('express').Router(); const c=require('../controllers/categoryController'); const {protect,adminOnly}=require('../middlewares/auth'); const upload=require('../middlewares/upload'); const validate=require('../middlewares/validate'); const v=require('../validators/commonValidator');
r.get('/',c.list); r.get('/:id',c.get); r.post('/',protect,adminOnly,v.category,validate,c.create); r.patch('/:id',protect,adminOnly,c.update); r.delete('/:id',protect,adminOnly,c.remove); r.patch('/:id/restore',protect,adminOnly,c.restore); r.post('/:id/image',protect,adminOnly,upload.single('image'),c.uploadImage); module.exports=r;
