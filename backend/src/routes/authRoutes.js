const r=require('express').Router(); const c=require('../controllers/authController'); const v=require('../validators/authValidator'); const validate=require('../middlewares/validate'); const {protect}=require('../middlewares/auth');
r.post('/register',v.register,validate,c.register); r.post('/login',v.login,validate,c.login); r.post('/refresh',c.refresh); r.post('/logout',protect,c.logout); r.get('/me',protect,c.me); r.patch('/profile',protect,c.updateProfile); r.delete('/account',protect,c.deleteAccount); r.post('/verify-email',v.emailOtp,validate,c.verifyEmail); r.post('/forgot-password',v.forgot,validate,c.forgotPassword); r.post('/reset-password',v.reset,validate,c.resetPassword); module.exports=r;
