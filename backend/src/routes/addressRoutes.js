const r=require('express').Router(); const c=require('../controllers/addressController'); const {protect}=require('../middlewares/auth'); const v=require('../validators/commonValidator'); const validate=require('../middlewares/validate'); r.use(protect); r.get('/',c.list); r.post('/',v.address,validate,c.add); r.patch('/:id',c.update); r.delete('/:id',c.remove); r.patch('/:id/default',c.default); module.exports=r;
