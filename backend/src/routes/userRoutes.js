const r=require('express').Router(); const c=require('../controllers/userController'); const {protect,adminOnly}=require('../middlewares/auth'); r.use(protect,adminOnly); r.get('/',c.list); r.get('/export',c.export); r.get('/:id',c.get); r.patch('/:id',c.update); r.patch('/:id/block',c.block); r.patch('/:id/unblock',c.unblock); r.patch('/:id/deactivate',c.deactivate); module.exports=r;
